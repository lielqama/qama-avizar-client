import { SortByCustNamePipe } from './sort-by-cust-name.pipe';

describe('SortByCustNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByCustNamePipe();
    expect(pipe).toBeTruthy();
  });
});
