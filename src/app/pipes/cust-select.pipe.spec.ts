import { CustSelectPipe } from './cust-select.pipe';

describe('CustSelectPipe', () => {
  it('create an instance', () => {
    const pipe = new CustSelectPipe();
    expect(pipe).toBeTruthy();
  });
});
