import { FilterbyvaluePipe } from './filterbyvalue.pipe';

describe('FilterbyvaluePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbyvaluePipe();
    expect(pipe).toBeTruthy();
  });
});
