import { LimitListPipe } from './limit-list.pipe';

describe('LimitListPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitListPipe();
    expect(pipe).toBeTruthy();
  });
});
