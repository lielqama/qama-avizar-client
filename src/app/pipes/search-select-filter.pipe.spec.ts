import { SearchSelectFilterPipe } from './search-select-filter.pipe';

describe('SearchSelectFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSelectFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
